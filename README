UPD1: В папку tests добавлены тесты, на которых парсер не сработал в прошлый раз (tests/1, tests/2, tests/3).

Тестировалось на Ubuntu и Debian

Сборка:<br>
1. Запустить скрипт сборки командой ./b в папке проекта через терминал

Запуск:<br>
Лексер запускается командой через терминал в папке проекта
  1. Без фильтрации комментариев: ./lexer путь-к-файлу
  2. С фильтрацией комментариев: ./lexer -filter путь-к-файлу

Парсер запускается командой через терминал в папке проекта
python main.py [filename]
Пример запуска: python main.py prog.txt

Тесты расположены в папке tests
